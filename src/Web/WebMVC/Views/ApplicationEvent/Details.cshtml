@model Subscribing.Application.Queries.ViewModels.ApplicationEventViewModel

@{
    ViewData["Title"] = "Application Event Details";
    var headerList = new List<Header>() {
        new Header() { Controller = "Home", Text = "Back to Home" },
        new Header() { Text = " / " },
        new Header() { Controller = "ApplicationEvent", Text = "Application Event" } };
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-sm-4">
        <h2> Application Event Details</h2>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a class="" asp-controller="Home" asp-action="Index">Home</a>
            </li>
            <li class="breadcrumb-item">
                <a>Settings</a>
            </li>
            <li class="active breadcrumb-item">
                <strong>Application Event Details</strong>
            </li>
        </ol>
    </div>
</div>

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>Application Event Details</h5>
                </div>
                <div class="ibox-content">
                    <dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.ApplicationEventName)
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.ApplicationEventName)
                        </dd>
                        <dt>
                            @Html.DisplayNameFor(model => model.Description)
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.Description)
                        </dd>
                        
                    </dl>
                    <a class="btn btn-primary" asp-controller="ApplicationEvent" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
                    <a class="btn btn-white" asp-controller="ApplicationEvent" asp-action="Index">Back To List</a>
                </div>
            </div>
        </div>
    </div>
</div>